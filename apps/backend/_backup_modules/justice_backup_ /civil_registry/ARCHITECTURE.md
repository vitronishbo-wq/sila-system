# Module Architecture

Domain: identity

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/identity

## Purpose

Gerir capacidades do dominio governamental `identity` no contexto federado `identity`.

## Core Responsibilities

- Modelar regras de negocio do modulo `identity`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- Nenhuma entidade identificada automaticamente em `domain`.

## Use Cases

- BaseEvent
- CitizenDocumentService
- CitizenFUC
- CitizenService
- CitizenValidated
- CitizenValidationFailed
- FrozenProfileQueries
- ImmutableAuditWriter
- ProfileQueries
- RequestCreate
- RequestResponse
- RequestService
- RequestState
- RequestWorkflow
- RoutingDecision
- RoutingEngine
- RoutingRule
- analysis_states
- create_citizen
- create_request
- create_request_model
- default_status
- escalate
- get_central_dashboard
- get_citizen
- get_citizen_by_email
- get_citizen_data
- get_citizen_documents
- get_citizen_requests
- get_commune_dashboard
- get_municipality_dashboard
- get_province_dashboard
- get_repository
- get_request
- get_user_from_citizen_id
- initial_states
- issue_document_from_request
- record_validation_failure
- record_validation_success
- resolve
- route
- terminal_states
- to_dict
- update_citizen
- update_state
- validate_citizen
- view_central_governance
- view_commune_active_requests
- view_municipality_supervision
- view_province_exceptions
- waiting_states

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /v1/identidade-civil/citizens/atestados
- GET /v1/identidade-civil/citizens/bi
- GET /v1/identidade-civil/citizens/certidoes
- GET /v1/identidade-civil/citizens/data/{citizen_id}
- GET /v1/identidade-civil/citizens/data/{citizen_id}/events
- GET /v1/identidade-civil/citizens/data/{citizen_id}/projection
- GET /v1/identidade-civil/citizens/events
- GET /v1/identidade-civil/citizens/invoices
- GET /v1/identidade-civil/citizens/notifications/unread-count
- GET /v1/identidade-civil/citizens/payments
- GET /v1/identidade-civil/citizens/profile
- GET /v1/identidade-civil/citizens/{citizen_id}/profile
- POST /v1/identidade-civil/citizens/validate/{citizen_id}

## Dependencies

- Sem dependencias externas de modulo detectadas.

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
