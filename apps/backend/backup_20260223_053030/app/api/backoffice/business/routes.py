from fastapi import APIRouter

router = APIRouter(prefix="/business", tags=["Backoffice Business"])

# Nota: Todas as rotas foram consolidadas em /api/admin/
# Este módulo permanece por compatibilidade de imports mas está vazio.

