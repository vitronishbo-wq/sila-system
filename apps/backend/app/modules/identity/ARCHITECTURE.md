# Module Architecture

Domain: identity

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/identity

## Purpose

Gerir capacidades do dominio governamental `identity` no contexto federado `unknown`.

## Core Responsibilities

- Modelar regras de negocio do modulo `identity`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- AggregateRepositoryPort
- AuditStatus
- DocumentType
- EntityStatus
- EntityType
- EventPublisherPort
- Identity
- IdentityAggregate
- IdentityID
- IdentityStatus
- InvalidSignatureError
- LifecycleStatus
- NotificationPriority
- OperationType
- ProcessStatus
- QRSignatureValidator
- QRVerificationService
- RiskLevel
- SortOrder
- TrustLevel
- TrustScore
- ValidationLevel

## Use Cases

- EnrollBiometricDataCommandHandler
- RequestIdentityDocumentCommandHandler
- VerifyIdentityDocumentCommandHandler
- handle

## Public API

- Nenhum endpoint detectado automaticamente no diretório `api`.

## Dependencies

- core

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
