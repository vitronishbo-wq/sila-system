# Module Architecture

Domain: operations

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/operations

## Purpose

Gerir capacidades do dominio governamental `operations` no contexto federado `core_system`.

## Core Responsibilities

- Modelar regras de negocio do modulo `operations`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api

## Key Entities

- OrderStatus
- PaymentStatus

## Use Cases

- OperationsService
- add_documents
- complete_order
- confirm_payment
- create_order
- generate_payment
- get_order
- get_receipt
- list_services
- submit_order

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /v1/orders/{order_id}
- GET /v1/orders/{order_id}/receipt
- GET /v1/services
- POST /v1/orders
- POST /v1/orders/{order_id}/complete
- POST /v1/orders/{order_id}/documents
- POST /v1/orders/{order_id}/submit
- POST /v1/payments/{order_id}/generate
- POST /v1/payments/{reference}/confirm

## Dependencies

- Sem dependencias externas de modulo detectadas.

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
