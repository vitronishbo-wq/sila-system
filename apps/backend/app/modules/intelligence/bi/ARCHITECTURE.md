# Module Architecture

Domain: bi

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/bi

## Purpose

Gerir capacidades do dominio governamental `bi` no contexto federado `core_system`.

## Core Responsibilities

- Modelar regras de negocio do modulo `bi`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- Dashboard
- Metric
- MetricSource
- Report

## Use Cases

- AnalyticsService
- DashboardService
- KPIService
- available_domains
- export_report
- get_consolidated_kpis
- get_dashboard_domain
- get_dashboard_executivo
- get_domain_kpis
- has_domain_source
- run_report
- schedule_report

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /v1/dashboards/assistencia
- GET /v1/dashboards/domain/{domain}
- GET /v1/dashboards/educacao
- GET /v1/dashboards/emprego
- GET /v1/dashboards/executivo
- GET /v1/dashboards/financas
- GET /v1/dashboards/financas-publicas
- GET /v1/dashboards/identidade
- GET /v1/dashboards/juventude
- GET /v1/dashboards/saude
- GET /v1/dashboards/service-requests
- GET /v1/dashboards/workflow
- GET /v1/domains
- GET /v1/kpis
- GET /v1/kpis/assistencia
- GET /v1/kpis/domain/{domain}
- GET /v1/kpis/educacao
- GET /v1/kpis/emprego
- GET /v1/kpis/financas
- GET /v1/kpis/financas-publicas
- GET /v1/kpis/identidade
- GET /v1/kpis/juventude
- GET /v1/kpis/saude
- GET /v1/kpis/service-requests
- GET /v1/kpis/workflow

## Dependencies

- financas
- financas_publicas
- statistics

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
