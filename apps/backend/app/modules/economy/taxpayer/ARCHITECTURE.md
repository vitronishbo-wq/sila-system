# Module Architecture

Domain: taxpayer

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/taxpayer

## Purpose

Gerir capacidades do dominio governamental `taxpayer` no contexto federado `economy`.

## Core Responsibilities

- Modelar regras de negocio do modulo `taxpayer`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api

## Key Entities

- AggregateEntity
- DeclarationStatus
- NIF
- PaymentStatus
- TaxAmount
- TaxCertificate
- TaxCertificateNumber
- TaxDebt
- TaxDebtCreated
- TaxDeclaration
- TaxDeclarationFiled
- TaxDeclarationNumber
- TaxPaid
- TaxPayment
- TaxPeriod
- TaxRegime
- TaxType
- Taxpayer
- TaxpayerCertificate
- TaxpayerDebt
- TaxpayerRegistered
- TaxpayerStatus

## Use Cases

- taxpayer_repository

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /v1/agt/health
- GET /v1/agt/search
- GET /v1/agt/{taxpayer_id}
- GET /v1/agt/{taxpayer_id}/certificates
- GET /v1/agt/{taxpayer_id}/debts
- GET /v1/agt/{taxpayer_id}/declarations
- GET /v1/agt/{taxpayer_id}/payments
- GET /v1/agt/{taxpayer_id}/summary
- PATCH /v1/agt/{taxpayer_id}
- POST /v1/agt/payments/{payment_id}/reverse
- POST /v1/agt/register
- POST /v1/agt/{taxpayer_id}/certificates
- POST /v1/agt/{taxpayer_id}/declarations

## Dependencies

- Sem dependencias externas de modulo detectadas.

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
