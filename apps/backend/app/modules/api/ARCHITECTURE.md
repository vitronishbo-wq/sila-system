# Module Architecture

Domain: api

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/api

## Purpose

Gerir capacidades do dominio governamental `api` no contexto federado `unknown`.

## Core Responsibilities

- Modelar regras de negocio do modulo `api`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- ApiAggregate
- ApiID
- AuditStatus
- DocumentType
- EntityStatus
- EntityType
- LifecycleStatus
- NotificationPriority
- OperationType
- ProcessStatus
- SortOrder
- ValidationLevel

## Use Cases

- IssueAPIAccessTokenCommandHandler
- ProcessAPIRequestCommandHandler
- RegisterAPIEndpointCommandHandler
- handle

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /

## Dependencies

- core

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
