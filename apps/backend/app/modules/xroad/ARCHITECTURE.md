# Module Architecture

Domain: xroad

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/xroad

## Purpose

Gerir capacidades do dominio governamental `xroad` no contexto federado `unknown`.

## Core Responsibilities

- Modelar regras de negocio do modulo `xroad`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- AuditEntry
- AuditStatus
- DocumentType
- EntityStatus
- EntityType
- LifecycleStatus
- NotificationPriority
- OperationType
- ProcessStatus
- SILAEnvelope
- SortOrder
- ValidationLevel
- XroadAggregate
- XroadID

## Use Cases

- ApproveXRoadServiceAccessCommandHandler
- InvokeXRoadServiceCommandHandler
- RegisterXRoadServiceCommandHandler
- handle

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /services/discovery
- POST /biometric-sync
- POST /health/notify-death
- POST /health/validate-bi-status
- POST /health/verify-citizenship
- POST /justice/batch-identity-validation
- POST /justice/check-death-status
- POST /justice/verify-birth-notice

## Dependencies

- core

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
