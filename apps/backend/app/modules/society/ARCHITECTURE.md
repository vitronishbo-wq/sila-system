# Module Architecture

Domain: society

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/society

## Purpose

Gerir capacidades do dominio governamental `society` no contexto federado `society`.

## Core Responsibilities

- Modelar regras de negocio do modulo `society`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- AuditStatus
- DocumentType
- EntityStatus
- EntityType
- LifecycleStatus
- NotificationPriority
- OperationType
- ProcessStatus
- SocietyAggregate
- SocietyError
- SocietyID
- SortOrder
- ValidationLevel

## Use Cases

- ApproveWelfareEligibilityCommandHandler
- ProcessWelfarePaymentCommandHandler
- SubmitWelfareApplicationCommandHandler
- handle

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /v1/ping

## Dependencies

- core

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
