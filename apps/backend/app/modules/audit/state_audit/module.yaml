name: state_audit
type: "sub_domain"
version: 1.0.0
status: alpha
owner: sila-core
description: State audit engine for financial and procurement oversight

exposes:
  api_routers:
    - app.modules.audit.state_audit.api.router:router
  public_models: []
  events_published: []

requires:
  domains:
    - economy.public_budget
    - procurement
  core_services:
    - persistence

subdomains: []
