# Module Architecture

Domain: workflow

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/workflow

## Purpose

Gerir capacidades do dominio governamental `workflow` no contexto federado `core_system`.

## Core Responsibilities

- Modelar regras de negocio do modulo `workflow`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- AssignmentType
- EntityType
- TaskPriority
- TaskStatus
- TransitionType
- WorkflowDefinition
- WorkflowHistory
- WorkflowInstance
- WorkflowState
- WorkflowStatus
- WorkflowTask
- WorkflowTransition

## Use Cases

- WorkflowEngine
- assign_task
- check_sla_breaches
- complete_task
- create_task
- execute_transition
- get_instance_timeline
- get_tasks_for_role
- get_tasks_for_user
- start_workflow

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- GET /instances/{instance_id}
- GET /instances/{instance_id}/timeline
- GET /tasks/my
- GET /tasks/pending
- POST /instances/{instance_id}/transitions
- POST /start
- POST /tasks/{task_id}/assign
- POST /tasks/{task_id}/complete

## Dependencies

- assistencia_social
- educacao
- emprego
- juventude
- saude

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
