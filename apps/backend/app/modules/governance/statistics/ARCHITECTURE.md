# Module Architecture

Domain: statistics

Path:
/home/dev03wsl/sila-system/apps/backend/app/modules/statistics

## Purpose

Gerir capacidades do dominio governamental `statistics` no contexto federado `core_system`.

## Core Responsibilities

- Modelar regras de negocio do modulo `statistics`.
- Orquestrar casos de uso na camada `application`.
- Expor capacidades publicas via camada `api`.

## Architecture

DDD + Clean Architecture

Layers:

- domain
- application
- infrastructure
- api
- tests

## Key Entities

- Aggregation
- AggregationMethod
- Statistic
- TimeSeriesPoint

## Use Cases

- AggregationService
- ForecastingService
- StatisticsService
- aggregate_avg
- aggregate_rate
- aggregate_sum
- bulk_record_values
- calculate_average
- calculate_min_max
- calculate_sum
- collect_all_metrics
- delete_series_by_period
- delete_statistic
- delete_timeseries
- detect_trend
- forecast_moving_average
- get_dashboard_kpis
- get_latest
- get_series
- get_series_by_period
- get_statistic_details
- get_statistics_summary
- group_by_dimension
- list_all_statistics
- list_statistics_by_module
- record_value
- register_statistic
- update_statistic

## Public API

Routers encontrados em `api/router.py` e arquivos `api/*router*.py`/`api/*routes*.py`:

- DELETE /{statistic_id}
- DELETE /{statistic_id}/series/{timeseries_id}
- GET /
- GET /analytics/summary
- GET /code/{code}
- GET /dashboard/kpis
- GET /{statistic_id}
- GET /{statistic_id}/analytics/average
- GET /{statistic_id}/analytics/minmax
- GET /{statistic_id}/analytics/sum
- GET /{statistic_id}/latest
- GET /{statistic_id}/period
- GET /{statistic_id}/series
- POST /
- POST /dashboard/collect
- POST /{statistic_id}/bulk-record
- POST /{statistic_id}/record
- PUT /{statistic_id}

## Dependencies

- assistencia_social
- educacao
- emprego
- identidade_civil
- identity
- juventude
- saude
- service_requests
- workflow

## Notes for AI Agents

Regras importantes para IA:

- Nao acessar outros dominios diretamente.
- Usar `application` services/handlers como orquestracao.
- Manter separacao DDD entre `domain`, `application`, `infrastructure` e `api`.

_Auto-generated by `scripts/ai/generate_module_architecture_docs.py`. Nao editar manualmente; regenerar via `make architecture-docs`._
