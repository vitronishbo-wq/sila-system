name: Legacy Internal CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]
  workflow_dispatch:

jobs:
  backend-guardrails:
    uses: ./.github/workflows/reusable-backend-guardrails.yml
